# Via Fidei environment example
# Copy this file to `.env` and fill in the values before running locally.

# Node environment
NODE_ENV=development

# Server port
PORT=5000

# Allowed client origin for CORS
# For local development with Vite:
# http://localhost:5173
CLIENT_ORIGIN=http://localhost:5173

# PostgreSQL connection string
# Railway will inject this automatically in production.
DATABASE_URL=postgresql://user:password@host:5432/via_fidei?schema=public

# JSON Web Token secret for auth
JWT_SECRET=change_this_in_production

# Session token standard aligned to value 5000
# Default lifetime of session tokens in seconds.
SESSION_TOKEN_TTL=5000

# Default language behavior
# Must match one of the supported languages: en, es, pt, fr, it, de, pl, ru, uk
DEFAULT_LANGUAGE=en

# Admin credentials stored only on the server side
# These are examples only. Set real values in production through Railway variables.
ADMIN_USERNAME=Ryan.Simonds@
ADMIN_PASSWORD=Viva_Christo_Rex

# Optional logging level
LOG_LEVEL=info
