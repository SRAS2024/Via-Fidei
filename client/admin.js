// Via Fidei admin bootstrap
// The full React shell, including the admin console, lives in main.js.
// When the current path starts with /admin, AppShell automatically
// renders the admin view with mission, About, notices, collage, and theme controls.

import "./main.js";
